# Use Railway's public proxy URL from the Postgres service (e.g. *.proxy.rlwy.net) for
# local Prisma and local API. The postgres.railway.internal host only works inside Railway.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
