/** @deprecated Import from `@hourly/shared` - aliases kept for gradual migration. */
export {
  demoOpportunities as mockOpportunities,
  getOpportunityById,
  getOpportunitiesByOrg,
} from '@hourly/shared';
