/** @deprecated Import from `@hourly/shared` - aliases kept for gradual migration. */
export {
  demoStudent as mockStudent,
  demoOrganizations as mockOrganizations,
  demoAttendance as mockAttendance,
  demoApplications as mockApplications,
  demoBadges as mockBadges,
  demoMessages as mockMessages,
  demoOrgStats as mockOrgStats,
  demoApplicants as mockApplicants,
} from '@hourly/shared';
